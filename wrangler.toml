name = "marivi-hf-proxy"
main = "worker.js"
compatibility_date = "2026-07-01"

# Variables públicas (no secretas). Puedes cambiarlas aquí.
[vars]
HF_MODEL = "Qwen/Qwen2.5-72B-Instruct"
# Cambia esto por la URL real de tu GitHub Pages para restringir el acceso,
# por ejemplo: "https://tu-usuario.github.io"
ALLOWED_ORIGIN = "*"

# HF_TOKEN NO va aquí. Se define como secreto con:
#   wrangler secret put HF_TOKEN
